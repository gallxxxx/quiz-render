// Ручные настройки озвучки.
// Кнопка «8 Добавить озвучку» этот файл НЕ трогает — правки здесь сохраняются.

// Насколько громко звучит голос. 1 — как записано.
export const VOICE_VOLUME = 1;

// Через сколько кадров после начала раунда вступает голос.
// Пол-секунды идёт переход, поэтому даём ему чуть-чуть закончиться.
export const VOICE_DELAY_FRAMES = 8;

// Сколько секунд отрезать от НАЧАЛА записи.
// Нужно, когда в начале файла лишняя пауза или вдох.
//   intro  — фраза заставки
//   rounds — по номеру раунда: [0] это первый раунд
//   outro  — концовка
export const VOICE_TRIM_START = {
  intro: 0,
  rounds: [] as number[],
  outro: 0,
};

// Сколько секунд отрезать от КОНЦА записи.
// Нужно, когда в конце файла лишний хвост, вдох или пауза.
// Устроено так же, как VOICE_TRIM_START, только с другой стороны.
export const VOICE_TRIM_END = {
  intro: 0.1,
  rounds: [] as number[],
  outro: 0,
};
